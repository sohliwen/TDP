name: MyWorkFlow file

on:
  push:
    branches:
      - main

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest

    steps:
      - name: Print a greeting
        run: |
          echo "Hello there"



