  name: My Workflow File

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out the source code from the repository
      - name: Check out code
        uses: actions/checkout@v3

      # Step 2: Print a greeting message
      - name: Print a greeting
        run: |
          echo "Hello there"

      # Step 3: Set up Python (you can specify the version)
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10' # Set Python version (e.g., '3.9')]
      - run: python --version 

      # Step 4: Install dependencies (if you have a requirements.txt file)
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pytest
          

      - name: begin test
        run: |
          pytest
          
     # this is where we acccess the hidden repo
      - name: Hidden repo checkout 
        uses: actions/checkout@v4
        with:
           repository: sohliwen/UOBHidden
           path: hiddenfolder
           token: ${{ secrets.SECRETUOB }}

    # this prints out the hidden files!
      - name: print out hidden things 
        run: |
          ls
          cd hiddenfolder
          ls

    # create discord with discord action
      - name: Discord notification 
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        uses: Ilshidur/action-discord@master
        with:
          args: 'WHERE ARE WE EATING FOR LUNCH? BEN SAY HE TREATING'


    # build and test and deploy using YAML
      <!DOCTYPE html>
      <html lang="en">
      <head>
          <meta charset="UTF-8">
          <meta name="viewport" content="width=device-width, initial-scale=1.0">
          <title>Hello World</title>
          <style>
              body {
                font-family: Arial, sans-serif;
                text-align: center;
                margin-top: 50px;
              }
        </style>
      </head>
      <body>
           <h1>Hello, World!</h1>
           <p>This is a simple static webpage deployed to GitHub Pages.</p>
      </body>
      </html>

       
   
